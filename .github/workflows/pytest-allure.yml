name: Tests with Allure Report

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest allure-pytest allure-combine

    - name: Run tests with Allure
      run: pytest test_hello.py --alluredir=allure-results -v
      continue-on-error: true

    - name: Install Java for Allure CLI
      run: |
        sudo apt-get update
        sudo apt-get install -y default-jre

    - name: Download and Install Allure CLI
      run: |
        curl -L -o allure-2.21.0.zip https://github.com/allure-framework/allure2/releases/download/2.21.0/allure-2.21.0.zip
        unzip allure-2.21.0.zip

    - name: Generate Allure Report
      if: always()
      run: |
        ./allure-2.21.0/bin/allure generate allure-results --clean -o allure-report

    - name: Generate Self-Contained HTML Report
      if: always()
      run: |
        allure-combine allure-results --dest allure-report-single.html --auto-create-folders

    - name: Upload Full Allure Report as Artifact
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-report-full
        path: allure-report/
        retention-days: 30

    - name: Upload Self-Contained Report as Artifact
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-report-single-file
        path: allure-report-single.html
        retention-days: 30